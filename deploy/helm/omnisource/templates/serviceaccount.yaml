{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "omnisource.serviceAccountName" . }}
  labels:
    {{- include "omnisource.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
